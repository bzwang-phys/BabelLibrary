var cheerio = require("cheerio"),
    superagent = require("superagent"),
    http = require("http"),
    fs = require("fs"),
    
    url = require("url");

let promise = new Promise((resolve,reject)=>{

});

console.log(promise);
